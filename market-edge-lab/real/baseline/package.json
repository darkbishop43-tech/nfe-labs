{
  "name": "market-edge-baseline-real",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "check": "wrangler deploy --dry-run"
  },
  "dependencies": {
    "polymarket-us": "^0.1.1"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
