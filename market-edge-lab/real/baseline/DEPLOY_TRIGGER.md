# Baseline Real deployment trigger

This file intentionally contains no runtime logic, credentials, account data, funding instructions, or trading behavior.

Purpose: create a fresh commit on the isolated `market-edge-baseline-real` production branch after Cloudflare branch/root/deploy settings were corrected, so Cloudflare can demonstrate whether Git integration starts a new build from the correct branch.

The three existing Market Edge paper experiments remain frozen and untouched.
