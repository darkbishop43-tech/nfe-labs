name: Market Edge Paper Collector

on:
  # Updating this workflow bootstraps a collector window immediately instead of
  # waiting for GitHub's sometimes-delayed schedule event.
  push:
    branches:
      - main
    paths:
      - '.github/workflows/market-edge-paper-collector.yml'
  schedule:
    # GitHub schedule delivery can drift. A long-running guarded job supplies the
    # actual five-minute cadence, while a new scheduled window is requested every
    # four hours. The current runner lasts ~5h35m, leaving ~95 minutes of buffer
    # for a delayed schedule event before any gap can appear.
    - cron: '7 */4 * * *'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: market-edge-paper-collector
  cancel-in-progress: false

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 350
    steps:
      - name: Maintain five-minute paper collector cadence
        shell: bash
        run: |
          set -euo pipefail
          URL="https://market-edge-lab.darkbishop43.workers.dev/api/run"
          ITERATIONS=68
          INTERVAL_SECONDS=300

          echo "Starting guarded Market Edge paper collector window."
          echo "Paper only. No wallet, keys, signing, or live order submission."

          for i in $(seq 1 "$ITERATIONS"); do
            STARTED_AT=$(date -u +%Y-%m-%dT%H:%M:%SZ)
            echo "[$STARTED_AT] Collector call $i/$ITERATIONS"

            RESPONSE="$(curl --fail --show-error --silent \
              --retry 2 --retry-delay 5 \
              --connect-timeout 15 --max-time 90 \
              "$URL")"

            echo "$RESPONSE" | jq '{mode, status, collector, lastRunAt, balance, realizedPnl, trades, eligibleCount, rejectedCount, seenCount}'
            echo "$RESPONSE" | jq -e '.mode == "PAPER_ONLY" and (.lastRunAt != null)' >/dev/null

            if [ "$i" -lt "$ITERATIONS" ]; then
              sleep "$INTERVAL_SECONDS"
            fi
          done

          echo "Guarded collector window completed successfully."
